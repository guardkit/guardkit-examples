[pytest]
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
markers =
    asyncio: marks tests as async (deselect with '-m "not asyncio"')
addopts =
    -v
    --strict-markers
    --tb=short
